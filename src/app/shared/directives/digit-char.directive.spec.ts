import { DigitCharDirective } from './digit-char.directive';

describe('DigitCharDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitCharDirective();
    expect(directive).toBeTruthy();
  });
});
