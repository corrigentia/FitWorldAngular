import { InstructorDirective } from './instructor.directive';

describe('InstructorDirective', () => {
  it('should create an instance', () => {
    const directive = new InstructorDirective();
    expect(directive).toBeTruthy();
  });
});
