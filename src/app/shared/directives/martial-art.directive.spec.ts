import { MartialArtDirective } from './martial-art.directive';

describe('MartialArtDirective', () => {
  it('should create an instance', () => {
    const directive = new MartialArtDirective();
    expect(directive).toBeTruthy();
  });
});
