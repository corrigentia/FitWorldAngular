import { UniqueUsernameUpdateValidatorDirective } from './unique-username-update-validator.directive';

describe('UniqueUsernameUpdateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueUsernameUpdateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
