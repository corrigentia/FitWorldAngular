import { EquipmentDirective } from './equipment.directive';

describe('EquipmentDirective', () => {
  it('should create an instance', () => {
    const directive = new EquipmentDirective();
    expect(directive).toBeTruthy();
  });
});
