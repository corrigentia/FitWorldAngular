import { StudentService } from 'src/app/student/services/student.service';
import {
  UniqueUsernameValidator,
  UniqueUsernameValidatorDirective,
} from './username.directive';
import { TestBed } from '@angular/core/testing';

describe('UsernameDirective', () => {
  it('should create an instance', () => {
    // const service = TestBed.inject(StudentService);
    // const validator = new UniqueUsernameValidator(service);
    // const directive = new UniqueUsernameValidatorDirective(validator);
    // expect(directive).toBeTruthy();
  });
});
