import { UniqueDateTimeFilterPipe } from './unique-date-time-filter.pipe';

describe('UniqueDateTimeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueDateTimeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
